<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.scnu.springbootfinalhomework2024.controller.MovieController">
    <select id="getMovieByCountPlay" resultType="cn.scnu.springbootfinalhomework2024.entity.Movie">
        SELECT M.*, SUM(MP.play_one_day) AS total_play
        FROM movie M
        JOIN movie_playback MP ON M.movie_id = MP.movie_id
        GROUP BY M.movie_id
        ORDER BY total_play DESC
            LIMIT #{day}
    </select>


</mapper>